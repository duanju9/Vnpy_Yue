# 同花顺人气 Top100 × 5m 放量缩量策略：本批数据说明与结论

本目录专门收纳 **「THS 热度 CSV + 批量人气回测」** 这一批工作的背景、数据口径、**已落盘的回测结果**、可改进点，以及 **是否值得继续投入** 的判断依据。代码仍在仓库 `examples/quick_tests/`；**股票池 CSV 的副本**放在本目录 `测试成分股/` 内，便于 **单独打压缩包** 给他人阅读（详见 **`分发说明.md`**）。

---

## 1. 本批「数据」指什么

| 项目 | 说明 |
|------|------|
| **股票池** | 同花顺 App 热榜风格 Top100 本地快照（非实时）。**交付重跑命名**：`测试成分股/ths_hot_THS20260424-0707_交付重跑_20260508.csv`；**原文件名**：`测试成分股/ths_hot_top100_20260424_20260507.csv`；仓库另有 `examples/quick_tests/data/` 下副本（内容宜与上述一致） |
| **回测区间** | 脚本参数 **最近 10 个交易日**（`--last-n-sessions 10`），具体起止以各标的拉到的 5m K 与引擎切片为准 |
| **策略核心** | 5m：**放量涨 → 缩量跌 → 阳线买**（默认 `yang_after_bear`）；**次日** 5m MACD 死叉或当日收盘强平；同日一单、卖出日不再买 |
| **批量脚本** | `../qmt_batch_hot_rank_backtest.py`（汇总 Excel + 逐笔 sheet + JSONL 记录） |
| **单标的引擎** | `../qmt_5m_vol_pullback_macd_backtest.py` |

**注意**：人气榜用 CSV 固定截面，与「当日东财 proxy 拉榜」可并存；本批文档默认以 **该 THS CSV** 为股票池定义。

---

## 2. 回测结果归档（2026-05-07）

以下为本机一次完整三连跑（先无日线过滤，再 soft，再 strict）时，脚本 **控制台末尾截面统计** 与 **汇总 Excel 文件名**。三只脚本均 **成功 100/100**（指榜单内 100 只标的回测流程均跑完；其中有成交家数见下表「有成交家数」列）。

**共同参数（与脚本默认一致，除非另行说明）**

| 参数 | 取值 |
|------|------|
| `--top` | 100 |
| `--last-n-sessions` | 10 |
| `--rank-csv` | **交付重跑推荐**：`测试成分股/ths_hot_THS20260424-0707_交付重跑_20260508.csv`（与旧快照同内容、新文件名）；亦可沿用 `ths_hot_top100_20260424_20260507.csv` |
| `--buy-mode` | `yang_after_bear`（默认） |
| `--capital` | 100000（展示用本金） |
| `--lot` | 100（假设每笔股数） |
| `--commission-bps` | 2.5（单边佣金，万分之一为 1） |

**截面统计（脚本打印值，保留四位小数）**

| 配置 | 有成交家数 | 复利累计收益率% 均值 | 复利累计收益率% 中位数 | 胜率% 均值 | 胜率% 中位数 |
|------|------------|----------------------|------------------------|------------|--------------|
| 无 `--daily-phase1` | 100 | 4.7282 | 2.1643 | 59.4833 | 66.6667 |
| `--daily-phase1`（soft，默认） | 87 | 4.7619 | 2.4181 | 54.0996 | 66.6667 |
| `--daily-phase1 --daily-phase1-mode strict` | 80 | 3.5451 | 1.2807 | 57.2083 | 66.6667 |

**汇总 Excel 文件名**（相对仓库根目录 `Vnpy_Yue`：`examples/quick_tests/output/`；若你本机仓库不在 `D:\Vnpy\Vnpy_Yue`，请只替换盘符与上级路径，文件名与时间戳以你本地为准）

| 配置 | 汇总文件 |
|------|----------|
| 无日线阶段1 | `批量人气_最近10交易日_5m放量涨缩量跌_阳线买_次日MACD死叉_Top100_20260507_230928.xlsx`（历史，仓库 `output/`） |
| daily-phase1 soft | `批量人气_最近10交易日_5m放量涨缩量跌_阳线买_次日MACD死叉_Top100_20260507_230955.xlsx` |
| daily-phase1 strict | `批量人气_最近10交易日_5m放量涨缩量跌_阳线买_次日MACD死叉_Top100_20260507_230959.xlsx` |

**交付重跑（2026-05-07，本包 `产物/` 内，文件名带区分前缀）** — 详见 **`跑批记录_交付重跑_20260507_233707.md`**：

| 配置 | 本包 `产物/` 下文件名 |
|------|------------------------|
| 无日线 | `策略热股5m交付_THS20260424-0707_无日线_Top100_20260507_233707.xlsx` |
| 日线 soft | `策略热股5m交付_THS20260424-0707_日线阶段1soft_Top100_20260507_233707.xlsx` |
| 日线 strict | `策略热股5m交付_THS20260424-0707_日线阶段1strict_Top100_20260507_233707.xlsx` |

**逐笔与 JSONL**：各 Excel 内 **「逐笔」** sheet 含价上收益、毛盈亏、手续费、扣费后收益率、净盈亏等列；同次跑批还会在 `../output/backtest_runs.jsonl` 追加记录（若未关闭该功能）。

**简要解读**

- **soft**（昨收 > MA20）在本批上 **有成交家数减少**（100→87），截面复利均值/中位数与「无日线」同量级，略抬中位数。  
- **strict** 进一步减少成交家数（→80），截面复利均值与中位数 **均低于** 无日线与 soft，符合更严环境过滤的预期。  
- 更细的 **逐标的** 复利、笔数、时间线等以对应 Excel **汇总** sheet 为准。

### 整体策略展示与打分（热股 = 要波动；策略 = 求「相对稳定」的兑现）

本批 **不做 alpha 选股**，用同花顺人气池是为了 **波动与换手**，便于 5m 规则有发挥空间；「稳定套利」在此 **不承诺** 跑赢「同期一路持有」，而强调：**笔级胜率、过程回撤可控**，并在高波动环境里 **能重复兑现一部分价差**（与全程持有的取舍见 Excel 列 **策略复利减区间涨跌**）。

请运行（推荐：基于已生成的批量 Excel，**无需再拉 5m**，区间与打分与表内列一致）：

```text
python examples/quick_tests/策略热股5分钟-20260424_20260507/整体策略分析与打分.py ^
  --batch-xlsx examples/quick_tests/output/你的批量汇总.xlsx
```

会在本目录生成 **`整体策略展示与打分.md`**，内容包括：全池 **区间涨跌** 排序表、策略截面聚合、**0–100 策略打分**（收益力 / 波动里套利感 / 稳定性 / 回撤纪律）及等级 **A–D**。若仅有 CSV、尚未有 Excel，可用 `--interval-only` 现场拉 5m 扫区间（需 miniQMT）。

---

## 3. 逐笔 sheet 口径（「收益率」与「净盈亏」如何对齐）

历史上 **「单笔收益率」** 实为 **价上涨跌%**（不含手续费），与 **「本笔净盈亏元」**（已扣双边佣金）不是同一口径，用心算反推「隐含本金」会不一致。

当前批量逐笔列（见引擎 `batch_detail_rows`）建议理解为：

- **价上收益率**：仅价格 \((卖-买)/买×100\%\)，与 `Trade.pnl_pct` 一致。  
- **本笔毛盈亏元**：价差 × 手数，未扣费。  
- **本笔手续费元**：按 `--commission-bps` 双边从成交金额计。  
- **扣费后收益率**：`本笔净盈亏元 ÷ (买入价×手数) × 100%`，与净盈亏同一口径。  
- **本笔净盈亏元**：毛盈亏 − 手续费。

核对时用 **扣费后收益率 ↔ 本笔净盈亏元**；**价上收益率** 只描述价格路径，不必与净额线性对应。

---

## 4. 已能确定的事实（本批范围内）

1. **工程链路**：THS CSV → xtdata 5m → 规则引擎 → 汇总 + 逐笔 + `backtest_runs.jsonl` 可稳定跑通（依赖本机 miniQMT / 数据缓存）。  
2. **展示用复利**：按脚本约定的 **固定手数 `lot` + 展示本金 `capital`** 做复利连乘，是 **研究用标尺**，不是真实账户滑点与成交冲击模型。  
3. **截面统计**：是「多标的在同一日历窗口内各自复利」的横截面描述，**不是** walk-forward、也不是组合层面的资金分配优化结果。  
4. **热度数据**：CSV 为某段时间快照；与实时热榜、东财 proxy 榜单在 **成分与排名** 上均可存在差异，结论外推时需写明股票池来源。

---

## 5. 改进空间（按性价比大致排序）

| 方向 | 内容 | 难度 |
|------|------|------|
| **验证与可重复** | 对 `batch_detail_rows` 做少量 **单元测试**（给定 entry/exit/lot/bps，断言毛/费/净/扣费后收益率） | 低 |
| **样本外** | 滚动时间窗或 train/test 切分，避免同一窗口调参又同一窗口宣称「均值复利」 | 中 |
| **股票池** | 多段 CSV 或按日拉榜，观察策略是否 **依赖特定热度截面** | 中 |
| **仓位与成本** | 引入最小跳动、滑点、或按成交额比例仓位，观察 **扣费后** 分布变化 | 中～高 |
| **退出与过滤** | `macd_exit_skip_first_bars`、`entry_macd_bull`、`bear_last_close` 等已有开关做 **小规模网格** 并记录到 JSONL meta | 中 |
| **组合层** | 多标的资金分配、相关性、行业集中度（当前批量脚本未做） | 高 |

---

## 6. 是否「值得继续改进」——建议判断标准

**值得继续**，若你的目标是：

- 把本流水线做成 **可复现的研究脚手架**（换 CSV、换参数、自动落盘、对比截面）；  
- 在 **明确股票池与成本假设** 下，系统尝试过滤条件与出场规则；  
- 为后续接 **更严肃的回测框架**（组合、风险指标、样本外）打基础。

**不必过度投入在「调这一版规则直到截面均值最高」**，若：

- 期望在 **热榜截面 + 固定技术规则** 上直接得到稳定可实盘 alpha，而缺少 **样本外与执行层** 约束；  
- 无法获得与实盘一致的成本、停牌、涨跌停可成交性假设。

**一句话**：当前批次更适合定位为 **「数据 + 规则 + 报表」的可重复实验**，继续改进的价值主要在 **验证、样本外、成本与股票池敏感性**；单纯堆规则追截面均值，边际收益通常递减且易过拟合。

---

## 7. 相关路径速查

```
测试成分股/ths_hot_THS20260424-0707_交付重跑_20260508.csv   # 交付重跑用股票池快照（新文件名）
测试成分股/ths_hot_top100_20260424_20260507.csv             # 原快照，可并存
本包回测_THS三连跑.py          # 三连跑 → 写入 产物/（推荐）
源码/                          # 策略脚本副本（阅读用；运行见 源码/README.md）
产物/                          # 本包专用前缀 xlsx
整体策略分析与打分.py
整体策略展示与打分.md          # 默认输出；另有 整体策略展示与打分_交付重跑20260507.md 可并存
examples/quick_tests/qmt_batch_hot_rank_backtest.py   # 仓库内正式入口
examples/quick_tests/qmt_5m_vol_pullback_macd_backtest.py
examples/quick_tests/output/                   # 仓库默认 output（与本包 产物/ 区分）
```

复现 · **交付三连跑（推荐，产物进本包 `产物/`）**（在仓库 `Vnpy_Yue` 根目录）：

```text
python examples/quick_tests/策略热股5分钟-20260424_20260507/本包回测_THS三连跑.py
```

复现 · 单次手动（`--output-xlsx` 指向本包 `产物/` 亦可）：

```text
python examples/quick_tests/qmt_batch_hot_rank_backtest.py ^
  --rank-csv examples/quick_tests/策略热股5分钟-20260424_20260507/测试成分股/ths_hot_THS20260424-0707_交付重跑_20260508.csv ^
  --last-n-sessions 10 --top 100 ^
  --output-xlsx examples/quick_tests/策略热股5分钟-20260424_20260507/产物/自定义文件名.xlsx
```

---

## 8. 本目录维护约定

- 数值结论以 **当次 Excel / 控制台截面统计** 为准；第二节表格为 **已归档的一次跑批**；换数据或换日后请更新第二节，或另建 `跑批记录_YYYYMMDD.md`。  
- 跑批文件名-only 的简短备忘见：`跑批记录_20260507.md`。  
- **对外分发**：先看 **`分发说明.md`**（本包含什么、不含什么、建议 zip 命名）。  
- **交付重跑记录**：`跑批记录_交付重跑_20260507_233707.md`；更早一次：`跑批记录_20260507.md`。

## 9. 批量 Excel 汇总列：区间标尺（无选股、纯技术面）

批量脚本导出的 **「批量回测结果」** sheet 每行增加（与引擎 `batch_summary_cn` 一致）：

- **区间首开末收涨跌**：与回测 **同一截取后 5m**，第一根开盘价 → 最后一根收盘价的涨跌%%，作「同期一路持有」对照。  
- **策略复利减区间涨跌**：策略 **复利累计收益率** − 上列（百分点；负表示跑输全程持有）。

**「截面统计」** sheet 中增加上述指标的横截面均值/中位数（含「全体回测成功」与「仅成交&gt;0」子集）。单标的 `--xlsx` 的 **「回测摘要」** sheet 亦含对应两项（项目名带 `%` / `pt` 后缀）。

## 10. 本包脚本与生成物

| 文件 | 作用 |
|------|------|
| `本包回测_THS三连跑.py` | 调用仓库批量脚本，三连跑并将 xlsx 写入 **`产物/`**（文件名带 **`策略热股5m交付_THS20260424-0707_`**） |
| `源码/` | `qmt_5m_*`、`qmt_batch_*`、`backtest_recorder.py` **快照**，便于 zip 内阅读；运行说明见 **`源码/README.md`** |
| `产物/` | 交付专用 xlsx，见 **`产物/README.md`** |
| `整体策略分析与打分.py` | 读批量 xlsx → 写 **`整体策略展示与打分.md`**（可加 `--out-md` 另存区分版） |
| `整体策略展示与打分.md` | 默认分析输出 |
| `整体策略展示与打分_交付重跑20260507.md` | 与默认版同源时可作备份文件名 |
